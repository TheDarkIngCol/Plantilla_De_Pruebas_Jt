name: Taller de ejemplo

on:
  push:
    branches:
      - 'test/*'

env:
  NUMERO_1: ${{vars.VARIABLE_A}}
  NUMERO_2: ${{vars.VARIABLE_B}} 

jobs: 
  suma_de_numeros: 
    runs-on: ubuntu-latest
    steps:
      - name: Suma de números
        run: |
          suma=$(( NUMERO_1 + NUMERO_2))
          echo "La suma de ${{vars.VARIABLE_A}} y ${{vars.VARIABLE_B}} es $suma"
  comparacion_de_numeros:
    runs-on: ubuntu-latest
    steps:
      - name: Comparación de números
        run: | 
          if [ NUMERO_1 -gt NUMERO_2 ]; then
            echo "El numero NUMERO_1 es mayor que NUMERO_2"
          elif [ NUMERO_1 -lt NUMERO_2 ]; then
            echo "El numero NUMERO_1 es menor que NUMERO_2"
          else 
            echo "Los numeros son iguales"
          fi